# Email Configuration
EMAIL_ADDRESS=your_email@example.com
EMAIL_PASSWORD=your_app_password
EMAIL_SERVICE=gmail

# Optional: Custom SMTP Configuration (if not using predefined service)
# SMTP_SERVER=smtp.example.com
# SMTP_PORT=587
# USE_TLS=True
